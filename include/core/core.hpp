/* core.hpp
 * 
 * Class "Core", encapsulates the entire node graph. */
 
#ifndef CORE
#define CORE

#include <portaudio.h>
#include "core/wrappedConnectionTypes.hpp"
#include "core/node.hpp"

class Core {
    public:
        Core();
        
    private:
};

#endif
